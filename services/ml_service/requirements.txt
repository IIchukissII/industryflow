# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Core ML Libraries
scikit-learn==1.9.0
numpy==2.5.1
pandas==2.3.3
scipy==1.18.0
joblib==1.5.3

# Database
asyncpg==0.31.0
psycopg2-binary==2.9.12
redis[asyncio]==8.0.1

# Data Processing
# Not imported directly here — it backs pandas' and mlflow's Parquet IO. It canNOT track the
# cold-layer exporter's pin (services/cold_export, on 25): mlflow 3.14.0 requires
# `pyarrow<25,>=4.0.0`, so 25 makes this file unresolvable against the mlflow pinned below. 24 is
# what mlflow's own dependency closure settles on. Reading across the versions is fine and is what
# actually matters here — the cold layer's Parquet is format 2.6 (see cold_export/store.py), which
# every pyarrow in this range reads.
pyarrow==24.0.0

# Visualization & Analysis
matplotlib==3.11.0
seaborn==0.13.2

# MLOps & Experiment Tracking
mlflow==3.14.0

# Model Monitoring
# 0.4.40 (latest 0.4.x) lifts the pydantic<2 cap that 0.4.11 carried, so it co-installs with
# mlflow 3.x (pydantic>=2). Its numpy/pandas/scikit-learn floors are met by the pins above.
evidently==0.7.21

# Configuration
pyyaml==6.0.3
python-dotenv==1.2.2

# Utilities
tqdm==4.68.4
colorlog==6.10.1

# Object Storage (for model registry)
minio==7.2.20

# Metrics Export
prometheus-client==0.25.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
