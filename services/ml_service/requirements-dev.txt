# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Test-only dependencies for the feature-store unit tests (services/ml_service/tests).
# Minimal — the in-memory FeatureStore tests need the redis client importable (no running
# server; Redis-backed tests are marked `redis` and skipped in CI). numpy is pulled in because
# the feature_engineering package __init__ eagerly imports its engine module.
# numpy is only an indirect import here (the package __init__ loads the engine), so the dev
# pin just needs to install on the CI interpreter (py3.12) — it isn't exercised by these tests.
redis==5.0.1
numpy>=2.5.1
pytest==8.3.4
pytest-asyncio==0.24.0
# Model-drift tests (ADR-0021) exercise the real evidently compute path, and the drift package
# __init__ imports the scorer — so any `import drift` needs these. These match the api image pins:
# pandas drifted to 2.1.3 here while the api moved to 2.3.3, and 2.1.3 ships no cp314 wheel — on the
# 3.14 interpreter pip fell back to a source build and the install failed.
pandas==2.3.3
evidently==0.7.21
