# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Test-only dependencies for the feature-store unit tests (services/ml_service/tests).
# Minimal — the in-memory FeatureStore tests need the redis client importable (no running
# server; Redis-backed tests are marked `redis` and skipped in CI). numpy is pulled in because
# the feature_engineering package __init__ eagerly imports its engine module.
# numpy is only an indirect import here (the package __init__ loads the engine), so the dev
# pin just needs to install on the CI interpreter — it isn't exercised by these tests.
# redis matches the 8.x every runtime requirements file already pins; this dev pin was the last
# 5.x left in the tree, so the tests imported a different major than the services run.
redis==8.0.1
numpy>=2.5.1
pytest==9.1.1
pytest-asyncio==1.4.0
# Model-drift tests (ADR-0021) exercise the real evidently compute path, and the drift package
# __init__ imports the scorer — so any `import drift` needs these. These match the api image pins:
# pandas drifted to 2.1.3 here while the api moved to 2.3.3, and 2.1.3 ships no cp314 wheel — on the
# 3.14 interpreter pip fell back to a source build and the install failed.
pandas==2.3.3
evidently==0.7.21

# --- what it costs to drive a ROUTER, and why that is worth paying (ADR-0030) ------------------
# Until now this service's tests only ever imported pure modules, so the registration gate was
# never driven at the wire — and that gap had a price. The gate was keyed on a run and returned
# "unjudged" without one, so an uploaded artifact (which has no run, by decision) would have gone
# through it untouched with every pure test still green. A rule that nothing proves is CALLED is a
# rule that can be quietly disconnected.
#
# Importing one router costs the whole service, because `routers/__init__` imports every router
# eagerly: `models` itself needs only fastapi/pydantic/auth, but the package also pulls the MLflow
# client (registered-models, experiments), the metrics client (inference) and evidently (drift).
# That is the honest reason these are here rather than a wish for a lighter list. The tracking
# gateway's dev requirements carry fastapi + httpx for exactly the same purpose.
fastapi==0.139.0
httpx==0.28.1
python-jose[cryptography]==3.5.0
asyncpg==0.31.0
mlflow==3.14.0
prometheus-client==0.25.0
