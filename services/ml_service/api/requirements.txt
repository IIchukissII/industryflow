# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# FastAPI and web server.
# Held at 0.115.x to match the prometheus-fastapi-instrumentator (6.1.0) below: fastapi 0.138
# wraps included routers in `_IncludedRouter` objects with no `.path`, which the 6.1.0
# instrumentator's route-name resolver cannot introspect — it 500s every request, including
# /health. This is the same pinned combo api-gateway runs; 0.115 sits inside mlflow's fastapi<1.
fastapi==0.115.0
uvicorn[standard]==0.50.0
pydantic==2.13.4

# Database
asyncpg==0.31.0

# Authentication
python-jose[cryptography]==3.5.0

# Additional for API
python-multipart==0.0.32

# MLflow integration
mlflow==3.14.0
boto3==1.43.40

# ML libraries for inference
numpy==1.26.4
scikit-learn==1.9.0
xgboost==2.0.3

# Model-drift detection (ADR-0021): evidently computes the data/prediction drift score in
# the /api/drift endpoint; pandas frames the reference sample + current window. Matches the
# version already declared in the parent requirements.txt. Verified to resolve against the
# pinned fastapi 0.115 (evidently 0.7.21 does not force a fastapi upgrade).
pandas==2.1.3
evidently==0.7.21

# Feature Store
redis[asyncio]==5.0.1

# Prometheus metrics
prometheus-fastapi-instrumentator==6.1.0
