# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Tracking gateway runtime (ADR-0019): FastAPI app, httpx to MLflow, boto3 for artifact
# pre-signing, the shared store for capability resolution. The live MLflow/S3 path is
# cluster-validated (see README / issue #19).
fastapi==0.139.0
uvicorn==0.51.0
httpx==0.28.1
redis==8.0.1
boto3==1.43.46

# The artifact manifest is YAML; parsed with safe_load only (admission.py) — a gate that
# refuses code execution must not construct arbitrary Python while reading the upload.
PyYAML==6.0.3
