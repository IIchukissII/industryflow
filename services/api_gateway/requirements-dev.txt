# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Test-only dependencies (kept out of the runtime image — the Dockerfile installs
# requirements.txt only). Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
# Starlette's TestClient needs it (it raises at import without one). test_auth_surface.py drives the
# real fastapi-users routers through it — the only place the auth surface is actually exercised.
httpx2==2.5.0
