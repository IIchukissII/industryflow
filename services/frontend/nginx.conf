# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
# SPDX-License-Identifier: AGPL-3.0-or-later

# Redirect all plain HTTP to HTTPS (ADR-0004 dec 1).
# Unprivileged nginx listens on high ports (8080/8443); the published host ports stay 80/443.
server {
    listen 8080;
    server_name _;

    # Container healthcheck hits this over plain HTTP so it doesn't depend on the
    # self-signed cert; everything else is redirected to HTTPS.
    location = /healthz { add_header Content-Type text/plain; return 200 "ok\n"; }

    location / { return 301 https://$host$request_uri; }
}

server {
    listen 8443 ssl;
    http2 on;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # TLS: a real certificate is mounted at /etc/nginx/certs in production; the entrypoint
    # generates a self-signed one for local development if none is present.
    ssl_certificate     /etc/nginx/certs/tls.crt;
    ssl_certificate_key /etc/nginx/certs/tls.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    # ML service owns the model-registry, experiment and feature-config API surfaces; they live on
    # ml-service-api:8002, not the gateway. These more-specific prefixes win over the generic
    # /api/ below, keeping the app same-origin (ADR-0004 / ADR-0009). The tracking gateway serves
    # the *kernel's* MLflow traffic (ADR-0019); these are the browser's tenant-scoped read-paths.
    location /api/models             { proxy_pass http://ml-service-api:8002; }
    location /api/registered-models  { proxy_pass http://ml-service-api:8002; }
    location /api/experiments        { proxy_pass http://ml-service-api:8002; }
    location /api/feature-configs    { proxy_pass http://ml-service-api:8002; }
    # On-demand model-drift evaluation (ADR-0021): windowed evidently compute can run tens of
    # seconds, so give it a longer read timeout than the default 60s and keep it same-origin.
    location /api/drift {
        proxy_pass http://ml-service-api:8002;
        proxy_read_timeout 120s;
    }

    # Reverse-proxy the rest of the API to the gateway so the app is same-origin: cookies
    # are first-party and no CORS is needed (ADR-0004 / ADR-0009).
    location /api/         { proxy_pass http://api-gateway:8000; }
    location /auth/        { proxy_pass http://api-gateway:8000; }
    location /users/       { proxy_pass http://api-gateway:8000; }
    location = /health     { proxy_pass http://api-gateway:8000; }
    location /docs         { proxy_pass http://api-gateway:8000; }
    location /openapi.json { proxy_pass http://api-gateway:8000; }

    # WebSocket (sensor stream) — wss:// terminates here, proxied to the gateway.
    location /ws/ {
        proxy_pass http://api-gateway:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
    }

    # Embedded notebooks (ADR-0014 single-origin): the hub is mounted under /jupyter/ via its SSO
    # proxy, so it is same-origin with the app — the session cookie is first-party and the
    # /notebooks page can iframe it without cross-origin framing. The SSO proxy validates the
    # session against the gateway and forwards the verified identity to the hub. WebSocket-heavy
    # (Lab/kernel channels). A variable upstream + resolver keep the frontend booting even when the
    # opt-in notebooks profile is down (then /jupyter/ returns 502 rather than failing nginx start).
    location /jupyter/ {
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $notebook_sso notebook-sso:8080;
        proxy_pass http://$notebook_sso;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Cookie $http_cookie;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
    }

    # Single-page app: serve index.html for any non-file route (client-side routing).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
