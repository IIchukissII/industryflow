# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Test-only deps. binding.py / proxy.py / wire.py / scram.py are pure (no redis/postgres at
# import), so most tests need only pytest + pytest-asyncio. trustme generates a throwaway CA +
# server cert for the upstream-TLS integration test (a fake Postgres-TLS server, no real DB).
pytest==7.4.3
pytest-asyncio==0.21.1
trustme==1.2.1
