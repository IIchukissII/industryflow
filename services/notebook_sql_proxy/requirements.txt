# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Runtime deps for the SQL proxy (ADR-0015). The proxy is a transparent Postgres-wire relay
# (wire.py / scram.py / server.py), so it needs no Postgres driver — only the shared store client
# to resolve capability handles. The live relay is not yet cluster-validated (see README / #19).
redis==8.0.1
