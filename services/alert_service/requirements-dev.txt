# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Test-only dependencies. Minimal: importing the rules engine pulls in aiohttp; the rest of
# the runtime stack (Kafka/snappy/asyncpg) isn't needed for the pure threshold-rule tests.
aiohttp>=3.9.0
pytest==9.1.1
# The alert-rule model validators (ADR-0021 drift rules require a model_id + in-range drift
# threshold) are pure pydantic; test_alert_rule_models.py imports the api models. Matches the
# api pin (pydantic>=2.5.0).
pydantic>=2.5.0
