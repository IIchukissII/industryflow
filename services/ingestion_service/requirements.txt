# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# Held at 0.115.x: fastapi 0.138 wraps included routers in _IncludedRouter objects with
# no .path, which prometheus-fastapi-instrumentator 6.1.0 cannot introspect (every
# request, including /health, 500s). Same combo api-gateway/ml-service run.
fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.4

aiokafka==0.14.0

python-snappy>=0.7.0

# X.509 parsing of the verified device certificate forwarded by the mTLS edge (ADR-0002)
cryptography>=49.0.0

# Prometheus /metrics (request rate/latency/in-flight) — item 7 observability
prometheus-fastapi-instrumentator==6.1.0
