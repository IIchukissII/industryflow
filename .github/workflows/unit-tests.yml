# SPDX-FileCopyrightText: 2026 The IndustryFlow contributors
#
# SPDX-License-Identifier: AGPL-3.0-or-later

# In-process unit/functional tests that need no external services (no DB, broker, or edge).
# Database-backed proofs live in db-tenant-isolation.yml.
name: Unit tests

on:
  push:
    branches: [main]
    paths:
      - "services/**"
      - ".github/workflows/unit-tests.yml"
  pull_request:
    paths:
      - "services/**"
      - ".github/workflows/unit-tests.yml"

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { service: ingestion_service,   path: services/ingestion_service/tests }
          - { service: api_gateway,         path: services/api_gateway/tests }
          - { service: notebook_hub,        path: services/notebook_hub/tests }
          - { service: notebook_sql_proxy,  path: services/notebook_sql_proxy/tests }
          - { service: alert_service,       path: services/alert_service/tests }
          # Redis-backed tests are marked `redis` and skipped (no server in CI).
          - { service: ml_service,          path: services/ml_service/tests, args: "-m 'not redis'" }
          # Pure FK-resilience filter test only; test_spark_service.py is a live-DB harness.
          - { service: spark_jobs,          path: services/spark_jobs/tests/test_upsert_filter.py }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r services/${{ matrix.service }}/requirements-dev.txt
      - name: Run tests
        run: pytest ${{ matrix.path }} ${{ matrix.args }} -v
